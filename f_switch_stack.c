#include "monty.h"

/**
 * switch_to_stack - Switches the data structure to a stack (LIFO).
 * @stack: Stack head
 * @count: Line number
 */
void switch_to_stack(stack_t **stack, unsigned int count)
{
	(void)stack;
	(void)count;
	bus.lifi = 0;
}
